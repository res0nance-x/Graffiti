## TODO

- Blacklist/whitelist strategy for handling same author/recipient or general peer filtering:
  - Eventually implement a blacklist/whitelist system.
  - To avoid GUI clutter, start with a text-based version first (e.g., config file or simple text area input) rather than building out complex GUI elements immediately.
